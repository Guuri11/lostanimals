api_platform:
    title: 'Lost Animals'
    description: 'API Lost Animals'
    version: '1.0.0'
    mapping:
        paths: ['%kernel.project_dir%/src/Entity']
    patch_formats:
        json: ['application/merge-patch+json']
    swagger:
        api_keys:
             apiKey:
                name: Authorization
                type: header

        versions: [3]
